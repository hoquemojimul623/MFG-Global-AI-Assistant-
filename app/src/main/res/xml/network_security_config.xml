<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Block all unencrypted cleartext HTTP traffic to prevent MITM sniffing and packet interception -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
